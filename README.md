# Penugasan-HTML-CSS-ERC
Landing page registrasi "GatherUp 2026" menggunakan HTML5 semantik &amp; CSS3 murni. Fitur: layout responsif (Grid &amp; Flexbox), tipografi fluid clamp(), smooth scrolling, embed peta lokasi, serta formulir RSVP interaktif dengan validasi data bawaan.
